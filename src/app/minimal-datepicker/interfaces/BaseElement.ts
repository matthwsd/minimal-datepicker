export interface BaseElement {
  /**
   * Font color of an element, if applicable
   */
  color?: string;
  /**
   * Background of an element, if applicable
   */
  background?: string;
  /**
   * Element radius, if applicable
   */
  radius?: number;
}
